// Centralized branding — single source of truth for logo + name.
// Every panel/sidebar/header pulls from here so branding stays consistent.
import logoAsset from "@/assets/school-withleo-logo.png.asset.json";

export const APP_NAME = "School withleo";
export const APP_TAGLINE = "Library Management";
export const LOGO_URL = logoAsset.url;
